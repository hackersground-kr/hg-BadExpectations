# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: real_hackerground
metadata:
    template: azd-init@1.9.5
services:
    web:
        project: web
        host: containerapp
        language: ts
        dist: build
